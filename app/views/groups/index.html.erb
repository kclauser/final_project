<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">FP Logo</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><a href="#">Link <span class="sr-only">(current)</span></a></li>
        <li><a href="#">Link</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Login", signin_path, class: "btn btn-primary" %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<p id="notice"><%= notice %></p>

<h1 class="groups-title">Groups Dashboard</h1>

<% @groups.each do |group| %>
  <div class="col-me-4 col-centered group-container">
    <div class="group-image">
    <%= image_tag('ihavenoidea.jpg') %>
    </div>
    <h2 class="group-name-display"><%= link_to group.name, group_path(group) %></h2>
    <%= link_to 'Show', group %>
    <%= link_to 'Edit', edit_group_path(group) %>
    <%= link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' } %>
  </div>
<% end %>

<% @groups.each do |group| %>
  <div class="col-me-4 col-centered group-container">
    <div class="media">
      <div class="media-left">
        <%= image_tag(('ihavenoidea.jpg'), :class => "media-object") %>
      </div>
    </div>
    <div class="media-body">
    <h2 class="media-heading"><%= link_to group.name, group_path(group) %></h2>
      <%= link_to 'Show', group %>
      <%= link_to 'Edit', edit_group_path(group) %>
      <%= link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' } %>
    </div>
  </div>
<% end %>

<div class="media">
  <div class="media-left">
    <%= image_tag(('ihavenoidea.jpg'), :class => "media-object") %>
  </div>
  <div class="media-body">
    <h4 class="media-heading">Media heading</h4>
    ...
  </div>
</div>

<br>

<%= link_to 'New Group', new_group_path %>
